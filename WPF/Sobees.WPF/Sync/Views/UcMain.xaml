<UserControl x:Class="Sobees.Sync.Views.UcMain"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    >
  <DockPanel LastChildFill="True">
    <TextBlock Text="Register for a Sobees Account"
               DockPanel.Dock="Top" />
    <StackPanel DockPanel.Dock="Top">
      <TextBlock Text="Blablablablabla" />
      <TextBlock Text="Blablablablabla" />
      <TextBlock Text="Blablablablabla" />
      <TextBlock Text="Blablablablabla" />
      <TextBlock Text="Blablablablabla" />
    </StackPanel>
    <StackPanel Orientation="Horizontal">
      <Button Content="Create a new account"
              Command="{Binding CreateAccountCommand}" />
      <Button Content="Connect a account"
              Command="{Binding ConnectAccountCommand}" />
      <Button Content="Skip this"
              Command="{Binding CloseCommand}" />
    </StackPanel>


  </DockPanel>
</UserControl>
