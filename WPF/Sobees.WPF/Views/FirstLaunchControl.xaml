<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             x:Class="Sobees.Views.FirstLaunchControl"
             mc:Ignorable="d"
             DataContext="{Binding FirstLaunchControlViewModel, Source={StaticResource Locator}}"
             d:DesignWidth="938"
             d:DesignHeight="614">
  <Grid>
    <Border x:Name="geBg_FirstLaunch"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            CornerRadius="0,0,0,0"
            Grid.RowSpan="3">
      <Border.Background>
        <LinearGradientBrush EndPoint="0.5,1"
                             StartPoint="0.5,0">
          <GradientStop Color="#FFF4FBFF"
                        Offset="1" />
          <GradientStop Color="#FFC2E6FF"
                        Offset="0.328" />
        </LinearGradientBrush>
      </Border.Background>
      <Button Content="Finish"></Button>
    </Border>
  </Grid>
</UserControl>
